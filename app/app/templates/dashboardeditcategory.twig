{% extends "layout/dashboardlayout.twig" %}

{% block title %}
        <title>Slim 3</title>
{% endblock %}

{% block content %}
<div class="container">
    <h1 class="mt-4 mb-4">Edit Category : {{nom}}</h1>

    <div class="row">
        <div class="col-sm-6 col-md-4 col-md-offset-4 m-auto">
            <div class="account-wall">
              <form class="form-signin" action="{{ path_for('dashboardupdatecategory', {'id':id}) }}" method="post">
                    <input type="text" class="form-control" value="{{nom}}" name="name" required autofocus>

                    {{ csrf_fields() | raw }}
                    <button class="btn btn-lg btn-primary btn-block" type="submit">
                        Update</button>
                </form>
            </div>
        </div>
    </div>
</div>

{% endblock %}