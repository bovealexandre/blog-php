{% extends "layout/dashboardlayout.twig" %}

{% block title %}
        <title>Slim 3</title>
{% endblock %}

{% block content %}
<div class="container mt-4">
      <div class="row">
        <div class="col-lg-12">
          <p>

            <a href="{{path_for('dashboarduser')}}" class="btn btn-sq-lg btn-primary col-3">
                <i class="fa fa-user fa-5x"></i><br/>
                <span>Watch the</span> <br/>
                <span>users</span>
            </a>

            <a href="{{path_for('dashboardarticles')}}" class="btn btn-sq-lg btn-primary col-3">
                <i class="fa fa-pager fa-5x"></i><br/>
                <span>Watch the</span> <br/>
                <span>Articles</span>
            </a>

            <a href="{{path_for('dashboardcategories')}}" class="btn btn-sq-lg btn-primary col-3">
                <i class="fa fa-folder-open fa-5x"></i><br/>
                <span>Watch the</span> <br/>
                <span>categories</span>
            </a>
            
          </p>
        </div>
      </div>
</div>

{% endblock %}